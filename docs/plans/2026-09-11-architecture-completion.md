# 全项目架构优化实施与验收

用户于2026-09-11要求完成架构路线中的全部优化。基线为`b811723`，继续使用独立分支`codex/architecture-optimization`，不混入原工作区关系探索改动，不含生产发布。

## 实施批次

1. 统一任务接单/维护状态、重任务许可与root运行概况；保留四个领域worker和2核/4GB硬保护，默认不增加并发。
2. 抽取原子任务快照存储，修复创建/退款/写盘失败的证据丢失与重试；逐模块保留现有状态和文件格式。优先强化现有恢复协议，不为统一而强制迁移所有领域至同一状态机。
3. 标准发布流程涵盖四类任务、待补偿/损坏记录、数据库与运行文件备份、校验、阶段化恢复和收尾失败分类，提供隔离故障测试。
4. 前端API按领域拆分；统一可取消串行任务观察器，接入PPT/翻译/生图/婚恋，覆盖断线、慢响应、切任务、卸载、终态补偿。
5. 按职责拆分PPT和后台页面，保持视觉、路由与API兼容；针对新增/改动的管理写操作使用显式请求契约。
6. 拆出运行配置、存储路径与进程树回收基础能力，保持环境变量兼容；补充CI与受鉴权观测入口。
7. 全量Java/Node/前端构建、隔离MySQL、浏览器桌面/390/320、混合资源/真实产物验证，更新架构说明、AGENTS、WORKLOG与回滚边界后审查提交。

## 验收与风险

- 先定向验证每批行为，再运行受影响全量门禁；不使用生产数据库，不发起无限收费测试。真实环境与凭据若成为验收前提，先完成可独立验证工作再明确报告缺口。
- 接单关闭必须覆盖创建/重试/修订；资源等待可中断，取消不能重新运行，完成/失败释放许可；公开状态不得暴露用户资料。
- 退款前后保存失败均保留可恢复证据；跨文件/SQL故障用隔离MySQL确认，不将H2当作事务语义验收。
- 标准备份不得打印密钥或把生产数据放进可提交路径；安装失败与收尾统计失败分开判断，不覆盖上线后写入的数据。
- 前端契约保持Cookie/CSRF、幂等键、DOMPurify、sandbox、下载桥接与Blob释放；拆分页面用真实浏览器验证。
- 代码变更按批提交，回滚以当前状态格式兼容为前提；本轮不修改生产资源阈值、服务配置或运行数据。

## 批次状态与证据

- 批次1：已实现共享接单、重资源许可、运行汇总与独立后台面板；维护/取消等待/权限测试通过。
- 批次2：已抽取原子快照并增加失败重试、补偿清理保护、已退款任务恢复保护；翻译写盘故障和MySQL退款并发已验证。PPT/生图跨SQL和文件的更多故障组合仍需扩展。
- 批次3：已实现四模块排空、停机后SQL/运行目录/配置备份、校验与保守恢复边界。Python测试、生成脚本语法及隔离MySQL导出/导入通过；整机安装失败演练尚未完成。
- 批次4：已按领域拆API，四页面共用串行可取消轮询；PPT保留SSE。请求/观察器测试及构建通过。
- 批次5：PPT进度与后台运行面板已按职责独立；原PPT编辑/后台账号和provider表单仍待进一步拆分。
- 批次6：settings、RuntimePaths、ProcessTrees及VisionConfig已落地；CI工作流已添加，尚未远端执行。
- 批次7：Java/Node/前端构建、隔离MySQL与桌面/390/320页面检查已执行；真实混合资源产物、完整部署演练与原关系探索分支合并验收未完成。

验证仅针对独立架构工作区；接口夹具用于前端检查，不代表真实模型或生产验收。
